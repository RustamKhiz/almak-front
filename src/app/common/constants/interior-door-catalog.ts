export const INTERIOR_DOOR_WIDTH_OPTIONS = [40, 60, 65, 70, 75, 80, 85, 90] as const;
export const DEFAULT_INTERIOR_DOOR_WIDTH = 80;
export const DEFAULT_INTERIOR_DOOR_WIDTH2 = 40;

export const INTERIOR_DOOR_HEIGHT_OPTIONS = Array.from({ length: 13 }, (_, index) => 180 + index * 5);
export const DEFAULT_INTERIOR_DOOR_HEIGHT = 200;
